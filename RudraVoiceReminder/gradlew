#!/bin/sh
set -e
if command -v gradle >/dev/null 2>&1; then exec gradle "$@"; fi
echo "Gradle command not found. Use Codemagic's Android environment or generate the Gradle wrapper with ./gradle wrapper."
exit 1
